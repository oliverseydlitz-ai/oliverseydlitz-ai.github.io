# Next plan — 24 Sep 2026 (written overnight)

Written at the end of the 23→24 Sep overnight run. Most of the killer plan's high-severity work
is in its Done log. This file lists what is still left: the leftover killer-plan rows, new
findings from the night, and the questions only Oliver can answer.

**Rules for this plan.** Severity is HIGH (the app states something wrong, or breaks),
MED (it misleads, or a real user hits it), LOW (polish). "Small" means one render site, one
function or one copy line, with a test that fails on the old code. Anything that is a product
or design choice is a **question**, not a task.

---

## A. Small and unambiguous — do next (in this order)

**A1, A2 and A3 shipped at 04:15 CEST; A4 and A6 at 04:45** (all in `verdict-copy.js`); **A8 at 05:10** (`progress-ticks.js` — the fill half was already done by V9); **A7 at 05:50**, plus a zero change painted as a regression and a stray indent in the retention card, found in the same screenshot. **A5 at 06:15** (sample SD app-wide; the fatigue test drops its local correction). Section A is done.

| # | Sev | What is wrong | Fix |
|---|---|---|---|
| A1 | MED | **"✓ Clean" badge below the fault floor.** A session card shows the green Clean badge whenever `detectFaults` returns nothing — including sessions where no club reached `Metrics.MIN_SHOTS_REPORT`, so no fault *could* report. "Clean" is a verdict; "too few shots to judge" is not one. | Badge reads Clean only when at least one club cleared the floor; otherwise a neutral "Too few shots to judge". Test on a 6-shot session. |
| A2 | LOW | **"+0.0 vs 7-iron avg" is coloured as a gain.** The shot pop-up's comparison takes `d >= 0` as "up", so a delta that rounds to zero is painted green. | Neutral class when the rounded delta is 0. |
| A3 | LOW | **"1 more shots".** Three places pluralise `MIN_SHOTS_REPORT - n` unconditionally (Yardage row, the QuickStats message, the short-game builder). | One `plural(n, 'shot')` helper; test n = 1. |
| A4 | MED | **C49 copy, the unsourced superlatives.** "the #1 cause of scooping" (fault drill *Lead wrist flat*), "the single most common reason smash factor stays low" (Strike). Neither figure is in the research base. The "75% face" line in the D-plane note is the other known factual slip (the taught figure is 85%, and CLAUDE.md says it is driver-only). | Rewrite as plain claims with no rank ("a common cause of…"); correct the face-percentage line against `docs/coaching-calibration-audit.html`. |
| A5 | MED | **Global `stdDev()` is the population form (÷n).** Fixed locally in the fatigue test (C37), where it inflated false alarms from 5% to 12%. Everywhere else it slightly understates spread, most at small n (at n = 10 it reads 5% low). | Switch to n−1, run the full suite, and look at every test that pins a spread value; each changed figure should be explainable by the correction alone. Medium-sized because of the knock-on effects, not because of the code. |
| A6 | LOW | **Truncated "Premium (own l…" option** in the import form at 393px. | Shorter option label ("Premium (own)"), with the full wording in the description line under the menu. |
| A7 | LOW | **V42** — the first three session-detail blocks don't share a left edge (x=32 vs 16). | Drop the extra horizontal margin; confirm with a render-scan edge check. |
| A8 | LOW | **V31** — Progress chart tick labels print five decimals; the attack-angle fill paints above the line. | Precision per metric; `fill: false`. |

## B. Leftover killer-plan rows — real work, one per session

Not done overnight because each is more than a check-in's worth, or needs a look first.

- ~~**C30 (HIGH)**~~ DONE 24 Sep — about 30 body or unmeasurable causes still render under "What the numbers show"
  (forearms, hands, over-the-top, trail foot, dynamic loft, fatigue…). Tag each cause explicitly,
  treat an untagged cause as body, and add the strings to the bulk test.
- ~~**C45 (MED)**~~ DONE 25 Sep — validation, date, visible without imports (24 Sep); one significance rule, rangeLink vs its caveat, the putts plateau (25 Sep). Was: Rounds: validation (133% GIR was accepted and made the diagnosis), a date
  field, visible before two imports, one significance threshold.
- ~~**C46 (MED)**~~ DONE 25 Sep — repeatability verdict, one-way sign test (24 Sep); the tail trend is on `Metrics.mdcOf` (25 Sep). Was: SwingDNA compares σ against the MDC of a mean, so its verdict flips with n;
  "3 of 3 go right" is called a pattern. One significance rule and a floor on one-way verdicts.
- ~~**R30 (MED)**~~ DONE 24 Sep — Back leaves the app from any view. Write the hash in `Router.go`, handle
  `popstate`, route the detail as `#session/<id>`.
- ~~**R31 (MED)**~~ DONE 25 Sep — stale renders dropped (24 Sep); local first, cloud merged behind (25 Sep). Was: signed in, out-of-order cloud responses can show the wrong view. Drop stale
  renders, render local first, merge cloud behind it.
- ~~**R25 (MED)**~~ DONE 24 Sep — "Kept on this device" is claimed when the storage write failed. Read the flag
  back after writing, or roll back with a reason.
- **R27 / R36 / R37** — first paint: a tiny render-blocking theme script (white flash for
  dark-scheme users), `defer` the vendor scripts, stale-while-revalidate for the shell.
- **R33 (MED)** — PARTLY DONE 24 Sep (nav icon-only < 380px; all views reflow at 320px); left: ~197px (200% zoom on a phone). Was: at 320px the nav labels overlap; at 200% zoom 5 of 7 views scroll sideways.
- ~~**V38 / V39**~~ DONE 24 Sep — the five runtime modals still use their own inline style and pool across balls;
  render them into the `.modal` shell and route their numbers through `conditionGroups`.
  (Shipped on `QuickStats.pick`'s group rather than `conditionGroups`' largest: these dialogs
  answer "how am I hitting it now", the same question as the home row and the benchmark.)
- ~~**V40**~~ DONE 24 Sep — plan cards look tappable and do nothing; the grid shows 3 blocks where the range
  card says "of 4". Open the range card at the tapped block; show the transfer block.
- **V41, V46, V47, V48, V49** — Settings built two ways; print dedupe; heading wrap at 393;
  circles in a zero-radius system; data-rights polish.
- ~~**C39 / C40**~~ DONE 25 Sep — C39 dropped (every fix was a range-ball gate v2 removed); C40's club half built (`bag-pooling.js`). Was: re-read under v2 before doing anything. The rows ask for range-ball gates
  that Phase 9 removed on purpose; what survives is the *pooling across clubs* half (Club
  Benchmarks, the club modal, Analytics' "improvement" on any positive delta, personal bests),
  which v2 still forbids.
- ~~**C33 follow-up**~~ DONE 25 Sep (`Analytics.progressWindows`) — the Progress trend is now one club, oldest first, above the floor, but it
  does not apply `Metrics.conditionWeight`. Under v2 it should weight range sessions at ×0.8
  rather than treat them the same as premium.

## C. Questions for Oliver (not decided overnight)

1. **The Data & Rights modal promises "within 48 hours" and "within 30 days".** Those are
   commitments about what *you* will do when someone emails. Keep, change, or remove? The
   30-day figure matches GDPR Art. 12(3); the 48-hour one is stricter than the law asks, and the
   app now makes it in two places.
2. **Everything already on the Oliver list:** section-heading style (V14), default theme and
   manifest colours (R16), the logo colour, the "TOUR" chip and the rename, collapsing the
   Practice view, the home screen's one fault / one form / one priority layout, the affiliate
   link, and the tuning numbers (tier rates 0.30/0.35/0.40, ×0.8, +0.05).
3. **R35 — a "system" theme option.** It is a small build once the default theme (R16) is
   decided, and it makes little sense before that decision.
4. **Guide pages:** the CSV-export screenshots and the photo slots are still open (the guide
   suite prints them every run).
5. **Supabase dashboard:** the redirect allowlist check and leaked-password protection are
   still yours (see CLAUDE.md, "Open, and NOT fixable from this repo").

## D. What the overnight run changed that the next session should know

- `showConfirm` now takes `okLabel` (every call passes a verb), `danger`, `cancelLabel` and
  `noCancel`; `showInfo()` exists for plain information. `confirm-verbs.js` fails on a bare
  confirm.
- `ScrollMotion.observe` returns a stop handle (or `null`), tracks pending observers and prunes
  detached ones; `chart().destroy()` ends its watch. `observer-leak.js` pins it, via the new
  `load({ before })` hook in `test/load.js` for browser APIs jsdom lacks.
- `Strike.zQuantile` / `tQuantile` exist and the fatigue test uses a Bessel-corrected, Bonferroni
  t-test; see A5 for the global `stdDev` it did not fix.
- The render scan now opens the measurement-reference modal and the import preview and measures
  their tables. `KNOWN_CLIPPED` still holds the detail tables the killer plan's 2.6 names.
