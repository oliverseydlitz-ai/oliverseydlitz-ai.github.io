Fixtures for the browser checks.

- `session.csv` — a clean Rapsodo export, driver + 7-iron, 74 shots.
- `realistic.csv` — a second session with different numbers, so a two-session
  trend has something real to compare.
- `bank.csv` — deliberately **not** a golf CSV. It is a bank statement, and
  the import flow must refuse it at the door rather than importing it as a
  session of nothing. Keep it.
