node_modules
node_modules/
package-lock.json

# served mirror built by test/browser/sync.sh
test/browser/site/

# Playwright MCP scratch (screenshots, snapshots)
.playwright-mcp/

# Secrets. Nothing of this shape has ever been committed (65 commits scanned,
# September 2026 audit) and this is here so that stays true by default rather
# than by luck.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
.supabase/
service-role*
.og-template.html
